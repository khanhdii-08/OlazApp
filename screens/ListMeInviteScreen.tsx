import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ListMeInviteScreen = () => {
  return (
    <View>
      <Text>ListMeInviteScreen</Text>
    </View>
  );
};

export default ListMeInviteScreen;

const styles = StyleSheet.create({});
